import axios from "axios";
import { useEffect, useRef, useState } from "react";
import io from "socket.io-client";

let socket: any;

type Message = {
  message: string;
  name: string;
}

const Xx = () => {
  
  return (
    <div></div>
  );

};

export default Xx;
